# Buatlah function max(a, b, c) yang digunakan untuk mencari nilai maksimum dari a, b, dan c, di mana ketiganya bertipe
# data bilangan.
data =
def max(*myData):
    maks = myData[0]

    for data in myData:
        if a > b:
            maks = data
        elif b < a:
            maks = data
        elif c < a:
            maks = data
        elif c > a:
            maks = data
        elif b > c:
            maks = data
        elif c > b:
            maks = data
    return print(maks)

